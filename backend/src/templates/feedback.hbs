<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Feedback Received - Statsor</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .container {
            background: white;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #4ADE80, #22C55E);
            color: white;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
            text-align: center;
        }
        .feedback-details {
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .user-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        .info-item {
            background: white;
            padding: 15px;
            border-radius: 6px;
            border-left: 4px solid #4ADE80;
        }
        .priority-badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
        }
        .priority-high {
            background: #fee2e2;
            color: #dc2626;
        }
        .priority-medium {
            background: #fef3c7;
            color: #d97706;
        }
        .priority-normal {
            background: #dcfce7;
            color: #16a34a;
        }
        .sentiment-badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 11px;
            font-weight: bold;
        }
        .sentiment-positive {
            background: #dcfce7;
            color: #16a34a;
        }
        .sentiment-negative {
            background: #fee2e2;
            color: #dc2626;
        }
        .sentiment-neutral {
            background: #f3f4f6;
            color: #6b7280;
        }
        .message-content {
            background: white;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            font-style: italic;
        }
        .technical-details {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            font-size: 12px;
            color: #6b7280;
        }
        .action-buttons {
            text-align: center;
            margin: 30px 0;
        }
        .btn {
            display: inline-block;
            padding: 12px 24px;
            margin: 0 10px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: bold;
            color: white;
        }
        .btn-primary {
            background: #4ADE80;
        }
        .btn-secondary {
            background: #6b7280;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔔 New Feedback Received</h1>
            <p>Priority: <span class="priority-badge priority-{{priority}}">{{priority}}</span></p>
        </div>

        <div class="user-info">
            <div class="info-item">
                <strong>👤 User:</strong><br>
                {{user.name}} ({{user.email}})
            </div>
            <div class="info-item">
                <strong>📂 Category:</strong><br>
                {{category}}
            </div>
            <div class="info-item">
                <strong>📅 Submitted:</strong><br>
                {{formatDate timestamp}}
            </div>
            <div class="info-item">
                <strong>🎯 Sentiment:</strong><br>
                <span class="sentiment-badge sentiment-{{sentiment.label}}">
                    {{sentiment.label}} ({{sentiment.score}})
                </span>
            </div>
        </div>

        <div class="feedback-details">
            <h3>💬 Feedback Message:</h3>
            <div class="message-content">
                "{{message}}"
            </div>
        </div>

        {{#if adminView}}
        <div class="technical-details">
            <h4>🔧 Technical Details:</h4>
            <ul>
                <li><strong>User ID:</strong> {{user.id}}</li>
                <li><strong>Timestamp:</strong> {{timestamp}}</li>
                <li><strong>User Agent:</strong> {{userAgent}}</li>
                <li><strong>IP Address:</strong> {{ipAddress}}</li>
                <li><strong>Sentiment Score:</strong> {{sentiment.score}} ({{sentiment.confidence}} confidence)</li>
                <li><strong>Priority:</strong> {{priority}}</li>
                <li><strong>Expected Response:</strong> {{responseTime}}</li>
            </ul>
        </div>

        <div class="action-buttons">
            <a href="mailto:{{user.email}}?subject=Re: Your Statsor Feedback" class="btn btn-primary">
                📧 Reply to User
            </a>
            <a href="{{appUrl}}/admin/feedback" class="btn btn-secondary">
                📊 View All Feedback
            </a>
        </div>
        {{/if}}

        <div style="margin-top: 30px; padding-top: 20px; border-top: 1px solid #e9ecef; text-align: center; color: #666; font-size: 14px;">
            <p>This feedback was automatically processed by the Statsor feedback system.</p>
            <p>© 2025 Statsor. All rights reserved.</p>
        </div>
    </div>
</body>
</html>